[package]
name = "alarmate"
version = "0.3.0"
authors = ["Adrian Kumpf"]
edition = "2021"

[dependencies]
reqwest = "0.12.12"
serde = { version = "1.0.217", features = ["derive"] }
serde_json = "1.0.135"
clap = { version = "4.5.26", features = ["derive", "env"], optional = true }
tokio = { version = "1.43.0", default-features = false, features = ["macros", "rt-multi-thread"], optional = true }
thiserror = "2.0.11"

[features]
build-binary = ["clap", "tokio"]

[[bin]]
name = "alarmate"
required-features = ["build-binary"]

[profile.release]
lto = true
strip = "debuginfo"
codegen-units = 1
